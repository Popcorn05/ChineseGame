<!-- Don't mess with any of this window stuff, not sure what it does except Title, width and height -->
<Window x:Class="ChineseGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChineseGame"
        mc:Ignorable="d"
        Title="Editor" Height="450" Width="800"> <!-- Keeping height and width at default as window reacts to size change -->
    
    <!-- Styling and stuff (similar to <head> in html) -->
    <Window.Resources>
        <!-- Styling for most buttons in editor -->
        <Style TargetType="Button" x:Key="EditorButton">
            <Setter Property="Background" Value="#DCD6F7"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
        </Style>
    </Window.Resources>

    <!-- Main layout -->
    <!-- Everything kept in grid so that margins automatically adjust -->
    <Grid>
        <!-- Setup body grid -->
        <!-- Setup auto adjust columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <!-- Setup auto adjust columns -->
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        
        <!-- Content -->
        <!-- Held inside another grid so that everything adjusts with main body grid margins -->
        <Grid Grid.Column="1" Grid.Row="1">
            <Button Content="Button" Grid.Column="1" Grid.Row="1"/>
        </Grid>
    </Grid>
    
</Window>
